    <form class="task-form" method="POST">
        <h4>task manager</h4>
        <div class="form-control">
          <input
            type="text"
            name="name"
            class="task-input"
            placeholder="e.g. wash dishes"
          />
          <button type="submit" class="btn submit-btn">submit</button>
        </div>
        <div class="form-alert"></div>
  </form>

  <% if(tasks?.length > 0){ %>
    <%- include('./partials/singleTask.ejs') %>
  <%   } %>
